import React from 'react'
import ReactDOM from 'react-dom/client'
import TFCPayApp from './components/TFCPayApp'
import './styles/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <TFCPayApp />
    </React.StrictMode>,
)
